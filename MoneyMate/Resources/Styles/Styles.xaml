<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- Container principal -->
    <Style x:Key="MainContainer" TargetType="VerticalStackLayout">
        <Setter Property="Padding" Value="16,24,16,16"/>
        <Setter Property="Spacing" Value="16"/>
        <Setter Property="BackgroundColor" Value="{StaticResource BackgroundLight}"/>
    </Style>

    <!-- Header guest -->
    <Style x:Key="HeaderGuest" TargetType="Grid">
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryLight}" />
        <Setter Property="Padding" Value="10" />
    </Style>

    <!-- Header user -->
    <Style x:Key="HeaderUser" TargetType="Grid" BasedOn="{StaticResource HeaderGuest}">
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryLight}" />
    </Style>

    <!-- Footer -->
    <Style x:Key="FooterStyle" TargetType="Grid">
        <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="HorizontalOptions" Value="Center" />
    </Style>

    <!-- Styles de Page -->
    <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
        <Setter Property="BackgroundColor" Value="{StaticResource BackgroundLight}" />
    </Style>

    <!-- Carte -->
    <Style x:Key="CardFrame" TargetType="Border">
        <Setter Property="BackgroundColor" Value="{StaticResource BackgroundLight}" />
        <Setter Property="Stroke" Value="Transparent" />
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="12" />
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="16,8" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#00000030"
                        Offset="0,3"
                        Radius="8"
                        Opacity="0.25" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Textes -->
    <Style x:Key="PageTitle" TargetType="Label">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource TextDark}" />
        <Setter Property="Margin" Value="16,24,16,8" />
    </Style>

    <Style x:Key="SectionTitle" TargetType="Label">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource Primary}" />
        <Setter Property="Margin" Value="16,16,16,8" />
    </Style>

    <Style x:Key="BodyText" TargetType="Label">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="TextColor" Value="{StaticResource TextDark}" />
        <Setter Property="LineBreakMode" Value="WordWrap" />
    </Style>

    <Style x:Key="CaptionText" TargetType="Label">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextColor" Value="{StaticResource TextMuted}" />
    </Style>

    <!-- Label de formulaire -->
    <Style x:Key="FormLabel" TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource Secondary}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Margin" Value="4,8,0,0" />
    </Style>

    <!-- Bouton principal -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
        <Setter Property="TextColor" Value="{StaticResource TextLight}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="CornerRadius" Value="25" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#00000040"
                        Offset="0,4"
                        Radius="25"
                        Opacity="0.35" />
            </Setter.Value>
        </Setter>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource Accent}" />
                            <Setter Property="Shadow">
                                <Setter.Value>
                                    <Shadow Brush="#00000040"
                                            Offset="0,1"
                                            Radius="12"
                                            Opacity="0.15" />
                                </Setter.Value>
                            </Setter>
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
                            <Setter Property="Opacity" Value="0.9" />
                            <Setter Property="Shadow">
                                <Setter.Value>
                                    <Shadow Brush="#00000033"
                                            Offset="0,3"
                                            Radius="6"
                                            Opacity="0.3" />
                                </Setter.Value>
                            </Setter>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!-- Bouton secondaire -->
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
    </Style>

    <!-- Bouton outline -->
    <Style x:Key="OutlineButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryLight}" />
        <Setter Property="TextColor" Value="{StaticResource TextLight}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="CornerRadius" Value="25" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#00000040"
                        Offset="0,4"
                        Radius="10"
                        Opacity="0.45" />
            </Setter.Value>
        </Setter>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
                            <Setter Property="Shadow">
                                <Setter.Value>
                                    <Shadow Brush="#00000040"
                                            Offset="0,6"
                                            Radius="12"
                                            Opacity="0.5" />
                                </Setter.Value>
                            </Setter>
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
                            <Setter Property="Opacity" Value="0.9" />
                            <Setter Property="Shadow">
                                <Setter.Value>
                                    <Shadow Brush="#00000020"
                                            Offset="0,2"
                                            Radius="6"
                                            Opacity="0.2" />
                                </Setter.Value>
                            </Setter>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!-- Conteneur de champ -->
    <Style x:Key="InputContainer" TargetType="Border">
        <Setter Property="Stroke" Value="Transparent" />
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="20" />
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#00000030"
                        Offset="0,3"
                        Radius="8"
                        Opacity="0.25" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Entry interieure -->
    <Style x:Key="EntryForm" TargetType="Entry">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource TextDark}" />
        <Setter Property="PlaceholderColor" Value="{StaticResource Secondary}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="HeightRequest" Value="40" />
    </Style>

    <!-- Entry standard (formulaires generaux) -->
    <Style x:Key="FormEntry" TargetType="Entry" BasedOn="{StaticResource EntryForm}">
        <Setter Property="Margin" Value="16,8" />
    </Style>

</ResourceDictionary>
